# Active MVP backend — no environment variables required.
# The service runs entirely on local JSON by default.
BACKEND=json

# -----------------------------------------------------------------------
# Future: AFFiNE backend (NOT active in current MVP iteration)
# Uncomment and configure only when AFFiNE integration is explicitly scoped.
# -----------------------------------------------------------------------
# BACKEND=affine
# AFFINE_MCP_URL=http://localhost:3001/mcp
# AFFINE_WORKSPACE_ID=your-workspace-id
# AFFINE_API_TOKEN=your-api-token
